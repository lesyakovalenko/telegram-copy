export enum AuthRoutes {
    profile = '/profile',
    friends = '/friends',
    chat = '/chat'
}

export enum NonAuthRoutes {
    login = '/login',
    register = '/register',
    unauthorized = '/unauthorized'
}